<html>
	<head>
		<title>MiProyecto</title>
	</head>
	<body>
		{% if user.is_authenticated %}
			<p>Soy {{ user.get_username }}</p>
		{% endif %}
	<nav>
		<ul>
			{% if user.is_authenticated %}
				<li><a href="{% url 'logout' %}">Logout</a></li>
			{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
						<li><a href="{% url 'login' %}">Login</a></li>
			{% endif %}
		</ul>
	</nav>
		{% if Viajes %}
			<table border="2">
				<tr>
					<td>continente</td>
					<td>pais</td>
					<td>lugar</td>
					<td>tipo</td>
					<td>precio</td>
					<td>plazas_disponibles</td>
					<td>Ediciones</td>
			{% if user.is_authenticated %}
					<td>reserva</td>
			{% endif %}
				</tr>
				{% for viaje in Viajes %}
					<tr>
						<td>{{ viaje.continente }}</td>
						<td>{{ viaje.pais }}</td>
						<td>{{ viaje.lugar }}</td>
						<td>{{ viaje.tipo }}</td>
						<td>{{ viaje.precio }}</td>
						<td>{{ viaje.plazas_disponibles }}</td>
						<td><a href="{% url 'ediciones' viaje.id %}">Ir a ediciones</a></td> 
			{% if user.is_authenticated %}
						<td> <button>Reservar</button> </td>
			{% endif %}
			
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>Página del cheriff</p>			
		{% endif %}
	</body>
</html>
